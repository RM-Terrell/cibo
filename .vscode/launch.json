{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch TUI App",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd",
            "cwd": "${workspaceFolder}/cmd",
            "console": "integratedTerminal"
        }
    ]
}

